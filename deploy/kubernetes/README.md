# Deploy Asperathos in a Kuberneter Cluster

If you have a running kubernetes cluster, you can deploy each of the asperathos components by using the manifests in this directory.

Type:

```bash
kubectl apply -f .
```
